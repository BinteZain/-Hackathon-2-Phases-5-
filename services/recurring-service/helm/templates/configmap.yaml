apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recurring-service.fullname" . }}-config
  labels:
    {{- include "recurring-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
